import React from "react";
import styled from "styled-components";

const AppMenu = () => {
  return <AppMenuContainer>AppMenu</AppMenuContainer>;
};

export default AppMenu;

const AppMenuContainer = styled.div``;
